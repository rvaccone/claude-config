# secrets — ~/.claude.json and its backups contain MCP API tokens
backups/
.claude.json
.claude.json.backup*

# ephemeral runtime state
cache/
daemon/
daemon.lock
daemon.log
daemon.status.json
debug/
downloads/
file-history/
history.jsonl
jobs/
paste-cache/
plans/
projects/
session-env/
sessions/
shell-snapshots/
statsig/
tasks/
telemetry/
todos/
stats-cache.json
policy-limits.json
.last-cleanup
.last-update-result.json

# plugin marketplace cache (auto-fetched, ~4.5 MB)
plugins/

# auto-downloaded skills cache
skills/

# image and misc caches
image-cache/

# settings backups
settings.json.bak.*

# claude internal subdirectory
.claude/

# local-only settings overrides
settings.local.json

# machine-specific generated chrome native-host wrapper (hardcodes home dir + version)
chrome/

# local runtime state
remote-settings.json

# macOS
.DS_Store
